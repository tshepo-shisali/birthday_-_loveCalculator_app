<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lovecalculation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoveCalculation">
        <activity android:name=".LastForLove"
            android:parentActivityName=".LoveCalculatorScreen"></activity>
        <activity android:name=".LastForBirthday"
            android:parentActivityName=".BirthdayGameScreen"/>
        <activity android:name=".LoveCalculatorScreen" />
        <activity android:name=".BirthdayGameScreen" />
        <activity android:name=".HomeScreen" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>